/**
 * Secret key used for signing and verifying JWT tokens.
 * @type {string}
 */
export const secret = "secret-token"

/**
 * The duration (in seconds) for which the JWT token is valid.
 * @type {number}
 */
export const expiresIn = 60